{
  "name": "surge-conf",
  "displayName": "Surge Configuration",
  "description": "Syntax highlighting and formatting for Surge configuration files",
  "version": "0.0.1",
  "publisher": "cyrus",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Programming Languages",
    "Formatters"
  ],
  "keywords": [
    "surge",
    "proxy",
    "network",
    "configuration"
  ],
  "extensionKind": [
    "ui",
    "workspace"
  ],
  "main": "./out/src/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "surge",
        "aliases": [
          "Surge",
          "surge"
        ],
        "extensions": [
          ".dconf",
          ".sgmodule"
        ],
        "firstLine": "^#!MANAGED-CONFIG|^\\[General\\]",
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "surge",
        "scopeName": "source.surge",
        "path": "./syntaxes/surge.tmLanguage.json"
      }
    ]
  },
  "scripts": {
    "compile": "bunx tsc -p ./",
    "watch": "bunx tsc -watch -p ./",
    "test": "bun run out/test/grammar.test.js && bun run out/test/formatter.test.js",
    "pretest": "bun run compile"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.0",
    "vscode-tmgrammar-test": "^0.1.3"
  }
}
